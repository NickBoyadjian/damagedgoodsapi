{
  "routes": [
    {
      "method": "GET",
      "path": "/cuts",
      "handler": "cut.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cuts/count",
      "handler": "cut.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cuts/:id",
      "handler": "cut.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/cuts",
      "handler": "cut.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cuts/:id",
      "handler": "cut.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cuts/:id",
      "handler": "cut.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
